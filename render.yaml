services:
  - type: web
    name: pilotpay
    runtime: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
